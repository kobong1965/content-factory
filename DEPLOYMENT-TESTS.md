# 0.1.30 部署实测记录

测试日：2026-09-18。宿主 Windows 11 Pro x64、i5-14600KF、约 32GB 内存。用户明确取消虚拟机测试。以下均为本机隔离测试，不能代表每一台同事电脑均已验收。

| 项目 | 结果与边界 |
|---|---|
| Python/业务/真实 FFmpeg 回归 | 675 项通过，236.53 秒；全部通过，没有跳过失败项。后续初始化资源补全另运行安装专项 12 项通过 |
| React 行为测试 | 26 个文件、137 项通过 |
| TypeScript 与 Tauri 原生构建 | 通过；实际窗口另行检查，未将构建等同交互验收 |
| 独立运行时 | Python 3.12.14；清理开发 PYTHONPATH、PATH 仅使用包内工具和 System32，服务启动、健康检查、SQLite 写入通过 |
| 中文/空格路径安装 | 安装 worker 实际解压三个 ZIP 到全新中文/空格目录，获取固定 Skill、导入检查、原子发布版本目录通过 |
| 安装 EXE | 实际执行 NSIS 安装器退出码 0；同版本重复安装不覆盖用户数据。测试模式在指定安装目录创建启动快捷方式，不替换原桌面入口和原安装注册信息 |
| 快捷方式启动 | 实际打开测试快捷方式，原生窗口显示 0.1.30 和“本机服务已连接” |
| 端口与目录隔离 | 默认端口被占用后选择其他端口的回归通过；服务身份包含版本/程序目录/数据目录，拒绝误接其他实例 |
| 安装后的媒体处理 | 使用合成测试视频完成转写、两段镜头切分，生成 3000ms 含字幕 MP4，原片哈希不变 |
| 安装后的大语音模型 | 离线 CPU 加载、1 秒静音处理通过，没有生成不存在的口播。这不是含糊中文口播准确率验收 |
| Skill | 从上游固定提交下载，SHA-256 通过；首次启动复制到独立用户目录，已有 Skill 不覆盖；仅固定提示被应用读取 |
| 内部配置 | 加密导出、隔离目录导入、重新 DPAPI 保护通过；已使用迁移配置做一次真实最小模型请求，HTTP 200 / 完整 JSON。未重复付费视频分析 |
| 安装失败保护 | 缺少组件、同尺寸损坏、ZIP 路径越界、重复安装、后补配置包、已有程序缺文件及空初始化清单共 12 项通过；新增问题先复现失败再修复 |
| 公开数据检查 | 源码 allowlist 与当前真实模型 Key 比对；程序 ZIP 12,060 个文件流式扫描通过，不包含视频、数据库、真实配置或交付口令 |

## 未验证/未完成

- 没有进行虚拟机、第二台电脑、重启后的实机验收。
- 目标机器完全未装 WebView2/VC++ 时的安装路径尚未在干净机器实测；微软离线安装组件已提供并确认签名有效。VC++ 缺失时请求系统提权，拒绝提权则安装失败且旧程序保留。
- 完整的“真实素材→付费深度分析→用户 Skill→真实剪辑质量审核”没有在新电脑上重新跑通；回归中的模型请求以本地模拟为主，最小真实调用另列。
- 应用内自动更新、任务期间更新协调、数据库迁移前备份、跨版本失败回退仍未完成。
- 原素材及分析历史的可迁移内部数据包尚未完成；不能把本公开套件当成含所有历史项目的交付包。
- 安装程序没有 Windows 代码签名；公布校验值不等于验证发布者身份。
- 不分发 Windows 中文字体文件，非中文系统的字幕字体回退尚未验收。

## 复跑

源码提供 `scripts/verify-release-python.ps1`、`scripts/probe-packaged-runtime.py`、`scripts/probe-packaged-asr.py`、`scripts/probe-installed-media.py` 和 `scripts/build-windows-installer.ps1`。

开发者先生成 scripts 下三个 synthetic fixture，并准备受校验的 whisper tiny 模型。测试时必须传入新的隔离目录，不使用真实用户数据库。`data/gold-set/manifest.json` 仅为空的验收槽位定义，不含用户素材。
