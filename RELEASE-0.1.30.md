# 0.1.30 Windows x64 部署验证版

本版将已验收业务版本的界面、后台、Python、FFmpeg/FFprobe、ASR/OCR 与本地语音模型整理为可迁移部署套件。

## 下载与安装

下载同一 Release 的 `content-factory-0.1.30-setup.exe` 和三个 `content-factory-0.1.30-*.zip`，放到同一个文件夹，双击 setup。ZIP 不需要手工解压。可选择安装位置，安装程序检查完整性、准备运行组件、创建桌面入口。

安装 Huashu 固定分析提示时需要访问作者的 GitHub 原始文件地址；未公开镜像未确认再分发许可的第三方文件。软件不执行上游脚本。

有内部配置包的同事将 `model-config.cfcfg` 一并放在安装程序旁，首次启动输入单独交付的口令；无需逐条填写模型地址和 API Key。该包与口令不在公开 Release。已有配置保留。云端请求的费用归原模型服务账户。

## 本版变化

- API 和 ASR 各自使用独立的 Python 3.12.14；运行时不依赖开发电脑 Python、Node、FFmpeg 或 Codex 目录。
- 自动启动本地服务，核对版本和数据目录身份；默认端口占用时换用空闲端口；界面连接对应端口。
- 用户工作数据与程序版本目录分开；安装损坏/缺文件时拒绝继续，不覆盖旧版本。
- 配置包采用口令派生密钥与认证加密，导入后按目标 Windows 用户重新保护。
- 修复中文路径下 FFmpeg Whisper 模型路径问题；缓存和导出目录由安装版配置。

## 验证与限制

已完成本机独立运行组件测试、前端 137 项、最终 Python/媒体/业务回归 675 项（含安装保护 11 项）。已通过安装器创建的快捷方式实际打开原生窗口，确认 0.1.30 与“本机服务已连接”。中文/空格安装路径、安装后后台健康检查、SQLite 持久化、离线语音模型与合成视频的转写/镜头切分/字幕 MP4 导出通过。详细范围见 DEPLOYMENT-TESTS.md。

尚未进行同事电脑、Windows 重启、旧业务数据库迁移及完整升级失败回退验收。虚拟机验收按项目负责人最新要求取消。安装程序未做 Windows 发布者代码签名；SHA-256 用于完整性检查，不等同身份签名。

本版不包含个人素材及分析历史。完整业务数据迁移包与应用内自动更新仍未完成，不能将此版本称为全部原始交付要求均已完成的正式验收版。此 Release 因此标记为预发布版本。

建议 Windows 11 x64、16GB+ 内存和 12GB+ 程序盘可用空间，另计视频空间；CPU 模式，无独立显卡要求。Windows 10 22H2 x64 尚未实机验证；不支持 32 位和 ARM64 包。
